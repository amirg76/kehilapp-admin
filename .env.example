# Base URL of the kehilapp API.
# The admin authenticates with a cookie, so this must be an origin the backend
# lists in ALLOWED_ORIGINS — a credentialed cross-origin request from anywhere
# else is refused (see backend src/middlewares/cors.js).
VITE_API_BASE_URL=http://localhost:5001
